#/bin/bash


INPUT="$1"



while getopts "d" opt; do
	case "${opt}" in
		d)
			echo "/home/vagrant/exam"
			tar -czf archive-name.tar.gz /home/vagrant/exam
			;;

		?)
			echo "test23"
			;;

	esac

done
shift $((OPTIND-1))
